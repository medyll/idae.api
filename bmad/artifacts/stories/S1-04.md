id: S1-04
title: Add generic REST endpoint tests and CRUD examples
estimate: 3
role: developer
description: |
  The project already handles IDQL requests; add coverage and examples
  for the generic REST `/api/*` handler. Create unit tests exercising GET,
  POST, and PATCH routes using sample schemas and verify JSON response
  formatting. Update the `examples/curl_examples.sh` file with REST commands.

status: done
acceptance_criteria:
  - Unit tests for `/api/<resource>` endpoints exist and pass.
  - curl examples demonstrate GET and POST working.
  - Status.yaml backlog is updated and dashboard reflects the story.