<?php


	/**
	 * Generated ORM class for the agent post-actions, bound to the `agent` table.
	 *
	 * Written from web/bin/config/class_model.php by App::write_classes_app().
	 * Every method below is still the template's stub. Note that the constructor
	 * passes a bare `agent` rather than the string `'agent'`, which PHP 8 rejects
	 * as an undefined constant - so instantiating this class raises an Error.
	 */
	class agent_post_act extends App {

		/**
		 * Binds the class to the `agent` table.
		 */
		function __construct() {
			parent::__construct(agent);

		}

		/**
		 * Stub.
		 *
		 * @return void
		 */
		function set(){

			return;
		}

		/**
		 * Stub.
		 *
		 * @return void
		 */
		function get(){

			return;
		}
		/**
		 * Stub.
		 *
		 * @return void
		 */
		function set_field(){

			return;
		}

		/**
		 * Stub.
		 *
		 * @return void
		 */
		function get_field(){

			return;
		}
	}